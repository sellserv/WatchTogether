# WatchParty Environment Configuration
# Copy this to .env and adjust values

# Port the frontend will be accessible on (proxied by NPM Plus)
APP_PORT=8080

# CORS origin - set to your domain when using NPM Plus
# Example: https://watch.yourdomain.com
CORS_ORIGIN=*

# Comma-separated list of Invidious instances for YouTube comments proxy
# (optional - defaults are built-in)
# INVIDIOUS_INSTANCES=vid.puffyan.us,inv.nadeko.net,invidious.nerdvpn.de
